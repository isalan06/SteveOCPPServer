version: "3.0"

volumes:
  db-data:
    external: false

services:

  app:
    build: .
    links:
      - "db:mariadb"
    volumes:
      - .:/code
    ports:
      - "8080:8080"
    depends_on:
      - db
